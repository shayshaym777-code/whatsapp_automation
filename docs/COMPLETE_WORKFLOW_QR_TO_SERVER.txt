🔄 הworkflow המלא: QR → Proxy → Token → Server
════════════════════════════════════════════════════════════════════════════

זה הנושא המלא של איך לעבוד עם מספר, פרוקסי, וטוקן מ-QR עד לשרת!

════════════════════════════════════════════════════════════════════════════
שלב 1: סרוק QR (על המכשיר)
════════════════════════════════════════════════════════════════════════════

כאשר סורקים QR ב-WhatsApp Web:

Device (Android):
  1. פתח WhatsApp
  2. Settings → Linked Devices
  3. הציג QR מ-Browser
  4. סרוק את ה-QR עם ה-Device
  
מה קורה בBackend:
  - WhatsApp יוצר Session
  - שמור ה-Session בFile: WhatsApp_Data/Databases/wa.db
  
════════════════════════════════════════════════════════════════════════════
שלב 2: הגדר Proxy (BEFORE סריקה!)
════════════════════════════════════════════════════════════════════════════

⚠️ CRITICAL: Proxy חייב להיות מוגדר BEFORE סריקת ה-QR!

אם אתה סורק QR בלי Proxy:
→ Session = IP ממקום כלשהו (לא תואם מדינה)
→ אחרי כן, כשנשלח הודעה בProxy אחר = בלוק!

════════════════════════════════════════════════════════════════════════════
שלב 2A: הגדר Proxy ברמת המכשיר (Device Settings)
════════════════════════════════════════════════════════════════════════════

Via ADB:
```bash
# סרוק כל מספר עם Proxy אחר
adb shell settings put global http_proxy "195.154.45.123:8080"
adb shell settings put global global_http_proxy_port 8080
adb shell settings put global global_http_proxy_host "195.154.45.123"

# אם צריך Auth:
adb shell settings put global http_proxy_user "user_us_1234"
adb shell settings put global http_proxy_pass "password123"
```

Via UI:
```
Settings → Network & Internet → Advanced → Proxy
  - Proxy Type: Manual
  - Proxy Host: 195.154.45.123
  - Port: 8080
  - Username: user_us_1234
  - Password: password123
```

════════════════════════════════════════════════════════════════════════════
שלב 3: סרוק QR עם Proxy מעודכן!
════════════════════════════════════════════════════════════════════════════

עכשיו כאשר סורקים QR:

Device (with Proxy):
  1. Browser פתוח ב-Proxy (195.154.45.123)
  2. סרוק QR
  3. Session יוצר עם ה-IP של ה-Proxy
  4. Result: WhatsApp רואה (+1 from USA IP) ✓

═══════════════════════════════════════════════════════════════════════════
שלב 4: חלץ את Token מה-Device
════════════════════════════════════════════════════════════════════════════

אחרי סריקה מוצלחת, Token נשמר ב:
  Device: /data/data/com.whatsapp/databases/wa.db

דרכים לחילוץ:

⚡ דרך 1: ADB Shell (מהר)
```bash
adb shell cat /data/data/com.whatsapp/databases/wa.db > token.db
```

⚡ דרך 2: Python Script (עם Oxylabs)
```python
import adb
from adb.adb_commands import AdbCommands

adb_client = AdbCommands()
device = adb_client.GetDevices()[0]

# חלץ token
output = device.Shell("cat /data/data/com.whatsapp/databases/wa.db")
print(f"Token: {output}")
```

⚡ דרך 3: C# Code
```csharp
var deviceId = "98.98.125.9:26993";
var result = ExecuteAdbCommand($"adb -s {deviceId} shell cat /data/data/com.whatsapp/databases/wa.db");
var token = ParseToken(result);
return token;
```

════════════════════════════════════════════════════════════════════════════
שלב 5: חלץ את Proxy שהוגדר
════════════════════════════════════════════════════════════════════════════

צריך להחזיק את הProxy Details שהגדרת ב-Step 2:

```csharp
// מה שהגדרת בDevices Settings:
var proxyInfo = new
{
    proxy_ip = "195.154.45.123",
    proxy_port = 8080,
    proxy_username = "user_us_1234",
    proxy_password = "password123",
    country = "USA"
};

// או קרא מה-Device:
var proxySettings = ExecuteAdbCommand(
    $"adb shell settings get global http_proxy"
);
// Result: "195.154.45.123:8080"
```

════════════════════════════════════════════════════════════════════════════
שלב 6: צור Object עם Token + Proxy
════════════════════════════════════════════════════════════════════════════

עכשיו יש לך שניים:
1. Token (מ-Step 4)
2. Proxy (מ-Step 5)

צור Package יחיד:

```csharp
var accountPackage = new
{
    // מה שחלצת
    phone_number = "+1-555-0123",
    session_token = "abc123xyz...",  // מה-Device
    
    // Proxy שהשתמשת
    proxy_ip = "195.154.45.123",      // אותו Proxy!
    proxy_port = 8080,
    proxy_username = "user_us_1234",
    proxy_password = "password123",
    
    // Metadata
    country = "USA",
    device_id = "Device-1",
    created_at = DateTime.Now,
    qr_scanned_at = DateTime.Now,
    verified = true
};
```

════════════════════════════════════════════════════════════════════════════
שלב 7: שלח את ה-Package לServer
════════════════════════════════════════════════════════════════════════════

עברת את Package המלא לServer:

```csharp
var json = JsonConvert.SerializeObject(accountPackage);
var content = new StringContent(json, Encoding.UTF8, "application/json");

using (var client = new HttpClient())
{
    var response = await client.PostAsync(
        "http://your-server.com:5000/api/accounts/create",
        content
    );
    
    if (response.IsSuccessStatusCode)
    {
        Console.WriteLine("✓ Account created on server!");
        Console.WriteLine($"  Phone: {accountPackage.phone_number}");
        Console.WriteLine($"  Proxy: {accountPackage.proxy_ip}");
        Console.WriteLine($"  Token: {accountPackage.session_token.Substring(0, 20)}...");
    }
}
```

════════════════════════════════════════════════════════════════════════════
שלב 8: Server שמור ו-track
════════════════════════════════════════════════════════════════════════════

Server מקבל:
```json
POST /api/accounts/create
{
  "phone_number": "+1-555-0123",
  "session_token": "abc123xyz...",
  "proxy_ip": "195.154.45.123",
  "proxy_port": 8080,
  "proxy_username": "user_us_1234",
  "proxy_password": "password123",
  "country": "USA"
}
```

Server שומר ל-Database (accounts.json):
```json
{
  "phone_number": "+1-555-0123",
  "session_token": "abc123xyz...",
  "proxy_ip": "195.154.45.123",
  "proxy_port": 8080,
  "proxy_username": "user_us_1234",
  "proxy_password": "password123",
  "status": "active",
  "created_at": "2025-11-22T14:30:00",
  "verified": true
}
```

════════════════════════════════════════════════════════════════════════════
שלב 9: כשServer שולח הודעה - השתמש בProxy הנכון!
════════════════════════════════════════════════════════════════════════════

Campaign Engine שולח הודעה:

```csharp
public async Task SendMessageToContacts(string phoneNumber, string message)
{
    // קרא Account מ-Database
    var account = GetAccount(phoneNumber);
    
    // בדוק ש-Proxy קיים:
    if (account == null || !account.IsProxyValid())
    {
        Console.WriteLine("✗ No proxy configured for this account!");
        return;
    }
    
    // צור Proxy Handler
    var proxy = new WebProxy(
        $"http://{account.ProxyUsername}:{account.ProxyPassword}@{account.ProxyIp}:{account.ProxyPort}"
    );
    
    var handler = new HttpClientHandler
    {
        Proxy = proxy,
        UseProxy = true
    };
    
    using (var client = new HttpClient(handler))
    {
        // כל בקשה עוברת דרך ה-Proxy ה-Correct!
        var response = await client.PostAsync(
            "https://api.whatsapp.com/send",
            new StringContent(JsonConvert.SerializeObject(new
            {
                phone = account.PhoneNumber,
                message = message,
                session_token = account.SessionToken
            }))
        );
        
        Console.WriteLine($"✓ Message sent via {account.ProxyIp}");
    }
}
```

════════════════════════════════════════════════════════════════════════════
Workflow Visual:
════════════════════════════════════════════════════════════════════════════

Device 1 (Cloud Phone):
  │
  ├─ Step 1: מוגדר Proxy
  │          (195.154.45.123:8080, user_us_1234)
  │
  ├─ Step 2: סוקר QR
  │          (WhatsApp Session יוצר עם ה-Proxy IP)
  │
  ├─ Step 3: חולץ Token
  │          "WEB_SESSION_ID=abc123xyz..."
  │
  ├─ Step 4: חזור ל-Device את:
  │          - Token
  │          - Proxy Info
  │
  └─→ שלח לServer:
      {
        phone_number: "+1-555-0123",
        session_token: "abc123xyz...",
        proxy_ip: "195.154.45.123",
        proxy_port: 8080,
        proxy_username: "user_us_1234",
        proxy_password: "password123"
      }
         │
         ▼
      Server:
      ├─ שומר Account עם Token + Proxy
      │
      └─ כשמשלח Campaign:
         ├─ קרא Account
         ├─ בנה HttpClient עם Proxy
         └─ שלח הודעה דרך ה-Proxy ✓

════════════════════════════════════════════════════════════════════════════
Code Example: Complete Flow
════════════════════════════════════════════════════════════════════════════

```csharp
public class CompleteWorkflow
{
    // Step 1: Device Setup
    public async Task SetupDeviceWithProxy(string deviceId, string proxyIp, 
        int proxyPort, string proxyUsername, string proxyPassword)
    {
        // הגדר Proxy ב-Device
        ExecuteAdbCommand($"adb -s {deviceId} shell settings put global http_proxy \"{proxyIp}:{proxyPort}\"");
        ExecuteAdbCommand($"adb -s {deviceId} shell settings put global http_proxy_user \"{proxyUsername}\"");
        ExecuteAdbCommand($"adb -s {deviceId} shell settings put global http_proxy_pass \"{proxyPassword}\"");
        
        Console.WriteLine($"✓ Device {deviceId} configured with proxy {proxyIp}");
    }
    
    // Step 2: Scan QR
    public async Task ScanQR(string deviceId, string browserUrl)
    {
        Console.WriteLine("🔄 User scans QR on device...");
        // User זה המספר הסורק QR עם Proxy שהגדרנו
        // WhatsApp Session יוצר עם ה-Proxy IP!
        await Task.Delay(5000); // חכה לסריקה
    }
    
    // Step 3: Extract Token
    public string ExtractToken(string deviceId)
    {
        var result = ExecuteAdbCommand($"adb -s {deviceId} shell cat /data/data/com.whatsapp/databases/wa.db");
        var token = ParseTokenFromDatabase(result);
        return token;
    }
    
    // Step 4: Create Account Package
    public object CreateAccountPackage(string phoneNumber, string token, 
        string proxyIp, int proxyPort, string proxyUsername, string proxyPassword)
    {
        return new
        {
            phone_number = phoneNumber,
            session_token = token,
            proxy_ip = proxyIp,
            proxy_port = proxyPort,
            proxy_username = proxyUsername,
            proxy_password = proxyPassword,
            country = ExtractCountryFromPhone(phoneNumber)
        };
    }
    
    // Step 5: Send to Server
    public async Task SendAccountToServer(object accountPackage, string serverUrl)
    {
        var json = JsonConvert.SerializeObject(accountPackage);
        var content = new StringContent(json, Encoding.UTF8, "application/json");
        
        using (var client = new HttpClient())
        {
            var response = await client.PostAsync($"{serverUrl}/api/accounts/create", content);
            
            if (response.IsSuccessStatusCode)
            {
                Console.WriteLine("✓ Account created on server!");
            }
            else
            {
                Console.WriteLine($"✗ Error: {response.StatusCode}");
            }
        }
    }
    
    // Main Execution
    public async Task RunCompleteWorkflow()
    {
        string deviceId = "98.98.125.9:26993";
        string phoneNumber = "+1-555-0123";
        string proxyIp = "195.154.45.123";
        string proxyPort = "8080";
        string proxyUsername = "user_us_1234";
        string proxyPassword = "password123";
        
        // Step 1: Setup Proxy
        await SetupDeviceWithProxy(deviceId, proxyIp, 8080, proxyUsername, proxyPassword);
        
        // Step 2: Scan QR (manual)
        await ScanQR(deviceId, "https://web.whatsapp.com");
        
        // Step 3: Extract Token
        var token = ExtractToken(deviceId);
        Console.WriteLine($"Token extracted: {token.Substring(0, 20)}...");
        
        // Step 4: Create Package
        var accountPackage = CreateAccountPackage(
            phoneNumber, token, proxyIp, 8080, proxyUsername, proxyPassword
        );
        
        // Step 5: Send to Server
        await SendAccountToServer(accountPackage, "http://your-server.com:5000");
        
        Console.WriteLine("✓ Complete workflow finished!");
    }
}
```

════════════════════════════════════════════════════════════════════════════
Important Rules:
════════════════════════════════════════════════════════════════════════════

✅ MUST DO:
1. Set Proxy BEFORE QR scan
2. QR scan = Session with Proxy IP
3. Token + Proxy = Must be SAME Proxy
4. Send BOTH Token + Proxy to Server
5. Server must use same Proxy when sending messages

✗ MUST NOT DO:
1. Scan QR without Proxy (Session = wrong IP)
2. Scan QR with Proxy A, then send with Proxy B
3. Send token without proxy info
4. Use server without proxy routing

════════════════════════════════════════════════════════════════════════════
Edge Cases:
════════════════════════════════════════════════════════════════════════════

❓ "What if I scan QR without proxy?"
→ Token is created with wrong IP
→ Later, when sending with correct proxy: WhatsApp blocks
→ Solution: Rescan QR with correct proxy

❓ "Can I change proxy after scanning?"
→ Technically yes, but:
   - Old: Token bound to Proxy A IP
   - New: Messages sent via Proxy B IP
   - WhatsApp: "Suspicious activity! Face-ID check!"
→ Solution: If changing, rescan QR

❓ "What if proxy dies mid-campaign?"
→ Messages start failing
→ WhatsApp may see IP switch = block
→ Solution: Have backup proxy IP ready

❓ "Do all 1000 accounts need different proxies?"
→ YES! Each account = unique IP
→ If 1000 accounts from same proxy:
   - WhatsApp: "1000 accounts from same IP!"
   - Result: Immediate ban of all 1000

════════════════════════════════════════════════════════════════════════════
Database Schema (Server Side):
════════════════════════════════════════════════════════════════════════════

```csharp
public class AccountDatabase
{
    public List<WhatsAppAccount> Accounts { get; set; }
}

public class WhatsAppAccount
{
    // Identity
    public string PhoneNumber { get; set; }
    public string SessionToken { get; set; }
    
    // Proxy Information (REQUIRED!)
    public string ProxyIp { get; set; }
    public int ProxyPort { get; set; }
    public string ProxyUsername { get; set; }
    public string ProxyPassword { get; set; }
    
    // Metadata
    public string Country { get; set; }
    public string ProxyProvider { get; set; }  // "Oxylabs"
    public DateTime CreatedAt { get; set; }
    public DateTime QrScannedAt { get; set; }
    public bool Verified { get; set; }
    public string Status { get; set; }  // "active", "paused", "blocked"
    
    // Methods
    public string GetProxyUrl()
    {
        return $"http://{ProxyUsername}:{ProxyPassword}@{ProxyIp}:{ProxyPort}";
    }
    
    public bool ValidateProxyMatch()
    {
        // בדוק ש-Proxy IP matches country
        var ipCountry = GeoIpService.GetCountry(ProxyIp);
        return ipCountry == Country;
    }
}
```

════════════════════════════════════════════════════════════════════════════
⚡ DEBUG MODE: הכנס מספר + QR ידנית (בשביל בדיקות!)
════════════════════════════════════════════════════════════════════════════

לצורכי Debug ו-Testing, אתה יכול להכניס מספר + Token ידנית בלי QR:

════════════════════════════════════════════════════════════════════════════
Debug Option 1: עדכן ישירות את accounts.json
════════════════════════════════════════════════════════════════════════════

```json
{
  "phone_number": "+1-555-0123",
  "session_token": "MANUAL_TEST_TOKEN_abc123xyz",
  "proxy_ip": "195.154.45.123",
  "proxy_port": 8080,
  "proxy_username": "user_us_1234",
  "proxy_password": "password123",
  "country": "USA",
  "proxy_provider": "Oxylabs",
  "created_at": "2025-11-22T14:30:00",
  "verified": true,
  "status": "active",
  "debug_mode": true,
  "manual_entry": true
}
```

השם הקובץ:
  accounts.json (in same folder as Server)
  
עדכן ו-Server יקרא את זה בפעם הבאה!

════════════════════════════════════════════════════════════════════════════
Debug Option 2: שלח ישירות לServer עם Postman / curl
════════════════════════════════════════════════════════════════════════════

```bash
curl -X POST http://localhost:5000/api/accounts/create \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "+1-555-0123",
    "session_token": "MANUAL_TEST_TOKEN_abc123xyz",
    "proxy_ip": "195.154.45.123",
    "proxy_port": 8080,
    "proxy_username": "user_us_1234",
    "proxy_password": "password123",
    "country": "USA"
  }'
```

Response יהיה:
```json
{
  "success": true,
  "message": "Account created successfully",
  "phone_number": "+1-555-0123",
  "status": "active"
}
```

════════════════════════════════════════════════════════════════════════════
Debug Option 3: C# Code - הכנס ידנית בתוך Program
════════════════════════════════════════════════════════════════════════════

```csharp
// בתוך Program.cs, בדוגמת Debug:

// ==================== DEBUG: Insert Manual Account ====================
app.MapPost("/debug/create-manual-account", async (HttpContext context) =>
{
    try
    {
        var manualAccount = new
        {
            phone_number = "+1-555-0123",
            session_token = "MANUAL_TEST_TOKEN_abc123xyz",
            proxy_ip = "195.154.45.123",
            proxy_port = 8080,
            proxy_username = "user_us_1234",
            proxy_password = "password123",
            country = "USA",
            created_at = DateTime.Now,
            verified = true,
            status = "active",
            debug = true
        };
        
        Console.WriteLine("\n🔧 [DEBUG] Manual Account Created:");
        Console.WriteLine($"   Phone: {manualAccount.phone_number}");
        Console.WriteLine($"   Token: {manualAccount.session_token}");
        Console.WriteLine($"   Proxy: {manualAccount.proxy_ip}:{manualAccount.proxy_port}");
        
        var json = JsonConvert.SerializeObject(manualAccount);
        await File.AppendAllTextAsync("accounts.json", json + "\n");
        
        context.Response.StatusCode = 201;
        await context.Response.WriteAsJsonAsync(new
        {
            success = true,
            message = "Debug account created manually",
            phone_number = manualAccount.phone_number
        });
    }
    catch (Exception ex)
    {
        context.Response.StatusCode = 400;
        await context.Response.WriteAsJsonAsync(new { error = ex.Message });
    }
});
```

אז קורים ל-Endpoint:
```bash
curl -X POST http://localhost:5000/debug/create-manual-account
```

════════════════════════════════════════════════════════════════════════════
Debug Option 4: Unity/Winforms UI - Input Form
════════════════════════════════════════════════════════════════════════════

```csharp
public partial class DebugForm : Form
{
    private TextBox phoneInput, tokenInput, proxyIpInput, proxyPortInput;
    private Button insertButton;
    
    public DebugForm()
    {
        InitializeComponent();
        SetupDebugUI();
    }
    
    private void SetupDebugUI()
    {
        // Phone Input
        phoneInput = new TextBox { PlaceholderText = "+1-555-0123", Width = 300 };
        
        // Token Input
        tokenInput = new TextBox { PlaceholderText = "Token...", Width = 300 };
        
        // Proxy IP Input
        proxyIpInput = new TextBox { PlaceholderText = "195.154.45.123", Width = 300 };
        
        // Proxy Port Input
        proxyPortInput = new TextBox { PlaceholderText = "8080", Width = 100 };
        
        // Insert Button
        insertButton = new Button { Text = "Insert Manual Account", Width = 200 };
        insertButton.Click += async (s, e) => await InsertManualAccount();
        
        // Add to form
        Controls.Add(new Label { Text = "Phone:" });
        Controls.Add(phoneInput);
        Controls.Add(new Label { Text = "Token:" });
        Controls.Add(tokenInput);
        Controls.Add(new Label { Text = "Proxy IP:" });
        Controls.Add(proxyIpInput);
        Controls.Add(new Label { Text = "Proxy Port:" });
        Controls.Add(proxyPortInput);
        Controls.Add(insertButton);
    }
    
    private async Task InsertManualAccount()
    {
        var account = new
        {
            phone_number = phoneInput.Text,
            session_token = tokenInput.Text,
            proxy_ip = proxyIpInput.Text,
            proxy_port = int.Parse(proxyPortInput.Text),
            proxy_username = "user_debug",
            proxy_password = "debug123",
            country = "USA",
            created_at = DateTime.Now,
            verified = true,
            status = "active",
            debug = true
        };
        
        var json = JsonConvert.SerializeObject(account);
        await File.AppendAllTextAsync("accounts.json", json + "\n");
        
        MessageBox.Show("✓ Account inserted successfully!");
    }
}
```

════════════════════════════════════════════════════════════════════════════
Debug Workflow:
════════════════════════════════════════════════════════════════════════════

1. בדוק אם Proxy עובד:
   ```bash
   curl -x http://user_us_1234:password123@195.154.45.123:8080 \
        https://api.ipify.org
   ```
   
   Response יהיה IP של ה-Proxy (לא שלך!)

2. הכנס Account ידנית:
   ```bash
   curl -X POST http://localhost:5000/debug/create-manual-account
   ```

3. בדוק אם Server קרא את זה:
   ```bash
   curl http://localhost:5000/api/accounts
   ```
   
   יראה את ה-Account בList

4. בדוק אם Proxy מעודכן בAccount:
   ```bash
   curl http://localhost:5000/api/accounts/get-proxy/+1-555-0123
   ```

5. נסה לשלוח הודעה דרך ה-Account:
   ```bash
   curl -X POST http://localhost:5000/api/messages/send \
     -d '{"phone": "+1-555-0123", "message": "Hello"}'
   ```

════════════════════════════════════════════════════════════════════════════
Debug vs Production:
════════════════════════════════════════════════════════════════════════════

Debug Mode (Testing):
  ✓ עדכן accounts.json ישירות
  ✓ הכנס Token/Proxy ידנית
  ✓ בדוק Flow בלי QR Scan
  ✓ בדוק Proxy Routing
  ✓ בדוק Message Sending

Production Mode (Real):
  ✓ Scanner QR עם Device
  ✓ Extract Token מ-wa.db
  ✓ שלח לServer דרך API
  ✓ Server מעדכן Database
  ✓ Campaign שולח דרך Proxy

════════════════════════════════════════════════════════════════════════════
Test Checklist:
════════════════════════════════════════════════════════════════════════════

□ Server running on :5000?
  curl http://localhost:5000/api/accounts

□ Manual account inserted?
  curl http://localhost:5000/api/accounts

□ Proxy details saved?
  curl http://localhost:5000/api/accounts | grep proxy_ip

□ Can read proxy from account?
  curl http://localhost:5000/api/accounts/get-proxy/+1-555-0123

□ HttpClient configured with proxy?
  (Check logs when sending message)

□ Message sent successfully?
  curl -X POST http://localhost:5000/api/messages/send

□ No IP mismatch errors?
  (WhatsApp should accept it)

════════════════════════════════════════════════════════════════════════════
Summary: The Complete Chain
════════════════════════════════════════════════════════════════════════════

Device Setup:
  Proxy FIRST ← CRITICAL!

QR Scan:
  WhatsApp Session = Proxy IP

Token Extraction:
  Extract from wa.db

Package Creation:
  { phone_number, session_token, proxy_ip, proxy_port, proxy_username, proxy_password }

Server Storage:
  Save Account with all info

Message Campaign:
  Read Account → Build HttpClient with Proxy → Send message
  
Result:
  All messages from correct IP ✓
  WhatsApp accepts all messages ✓
  Campaign succeeds ✓

════════════════════════════════════════════════════════════════════════════

DEBUG MODE:
  אם אתה רוצה לבדוק בלי QR - הכנס ידנית!
  ✓ עדכן accounts.json
  ✓ POST לServer דרך API
  ✓ או C# Form Input
  ✓ אז בדוק ש-Campaign משתמש בProxy הנכון

זה זה! כל ה-Workflow - מ-QR ועד לשרת, עם Proxy בכל שלב!
ובDebug Mode - תוכל לבדוק ללא QR!
